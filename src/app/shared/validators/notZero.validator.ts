// import { AbstractControl, ValidatorFn } from "@angular/forms";

// export function notZeroValidator(num: number): ValidatorFn {
//   return (control: AbstractControl): { [key: string]: any } => {
//     return num !== 0 ? { 'forbiddenName' : { value: control.value } } : null;
//   };
// }
